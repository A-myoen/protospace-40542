<main class="main">
  <div class="inner">
          <% if user_signed_in? %>
          <a href="/prototype/:prototype_id/users/<%= current_user.id %>">
          <%= "こんにちは#{current_user.name}さん" %>
          </a>
          <% else %>
          <% end %>
    <%# ログインしているときは以下を表示する %>
      <%# <div class="greeting"> %>
        <%# こんにちは、 %>
        <%# <%= link_to "ユーザー名さん", root_path, class: :greeting__link%>
      <%# </div> %>
    <%# // ログインしているときは上記を表示する %>
    <div class="card__wrapper">
      <%= render partial: 'prototype', collection: @prototypes %>
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
    </div>
  </div>
</main>
